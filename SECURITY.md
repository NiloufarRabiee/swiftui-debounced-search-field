# Security Policy

This package provides a local SwiftUI search-field component and task scheduling behavior. It does not perform networking, persistence, analytics, or authentication on its own.

Applications using the async search callback are responsible for handling their own network requests, credentials, and sensitive data safely.

For security-relevant issues, contact the repository owner privately through the contact information available on their GitHub profile.

Use GitHub Issues for ordinary bugs and feature requests.
